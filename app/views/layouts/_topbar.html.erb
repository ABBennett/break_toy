<nav class="top-bar" data-topbar role="navigation">
  <ul class="title-area">
    <li class="name">
      <h1><a href= <%= root_path %>>TalkingPoints</a></h1>
    </li>
     <!-- Remove the class "menu-icon" to get rid of menu icon. Take out "Menu" to just have icon alone -->
    <li class="toggle-topbar menu-icon"><a href="#"><span>Menu</span></a></li>
  </ul>

  <section class="top-bar-section">
    <!-- Right Nav Section -->
    <ul class="right">
      <% if user_signed_in? %>
        <%= content_tag :div, nil, class: "user_id", data: { user_id: "#{current_user.id}" } %>
        <li class="active"><%= link_to "Signed in as #{current_user.username}", user_path(current_user)%></li>
        <li><%= link_to 'Sign Out', destroy_user_session_path, method: :delete %></li>
      <% else %>
        <li class="has-dropdown">
          <a href="#">Acct</a>
          <ul class="dropdown">
            <li class="sign-in"><%= link_to 'Sign In', new_user_session_path %></li>
            <li class="active"><%= link_to 'Sign Up', new_user_registration_path %></li>
          </ul>
        </li>
      <% end %>
    </ul>

    <!-- Left Nav Section -->
    <!-- Left Nav Section -->
    <ul class="left">
      <li class="has-dropdown">
        <a href="/conversations">Talks</a>
        <ul class="dropdown">
          <li class="sign-in"><%= link_to "Classiest", conversations_path(order: "totalpoints") %></li>
          <li class="sign-in"><%= link_to "Chattiest", conversations_path(order: "message") %></li>
          <li class="sign-in"><%= link_to "Pointiest", conversations_path(order: "points") %></li>
          <li class="sign-in"><%= link_to "Top Sender", conversations_path(order: "sender") %></li>
          <li class="sign-in"><%= link_to "Top Recipient", conversations_path(order: "recipient") %></li>
          <li class="sign-in"><%= link_to "Lopsided", conversations_path(order: "lopsided") %></li>

        </ul>
      </li>
    </ul>
  </section>
</nav>
