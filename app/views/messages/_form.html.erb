<%= form_for [@conversation, @message], remote: :true do |f| %>
  <%= f.label :body, id: "message-label" %>
  <%= f.text_area :body, placeholder: "Say Something!" %>
  <%= f.submit "Send" %>
<% end %>
